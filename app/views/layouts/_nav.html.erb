<% if current_user %>
<nav class="navbar navbar-dark nav fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Budget App</a>
    <div class="navbar-brand fs-5"><%= title %></div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= button_to "Log out", destroy_user_session_path, method: :delete, data: { turbo: false }, class: "logout_button", class:"nav-link btn btn-secondary px-1 text-white"  %>
          </li>
      </ul>
    </div>
  </div>
</nav>
<%end%>